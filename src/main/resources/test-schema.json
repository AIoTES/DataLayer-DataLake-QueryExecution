{
    "Observation": {
		"type": "object",
		"properties": {
			"senderPlatform": {
				"type": "string", "description": "Id of the sender IoT platform"
			},
			"conversationId": {
				"type": "string", "description": "Id of the subscription"
			},
			"hasResult": {
				"type": "string", "description": "Result"
			},
			"featureOfInterest": {
				"type": "string", "description": "Feature of interest"
			},
			"observedProperty": {
				"type": "string", "description": "Type of measurement"
			},
			"phenomenonTime": {
				"type": "string", "description": "Timestamp"
			},
			"resultTime": {
				"type": "string", "description": "Date and time of the measurement"
			},
			"madeBysensor": {
				"type": "string", "description": "Id of the sensor"
			},
			"hasLocation": {
				"type": "string", "description": "Id of the location"
			},
			"ioTDeviceId": {
				"type": "string", "description": "Id of the device that hosts the sensor"
			},
			"featuresOfInterest": {
				"type": "string", "description": "Features of interest"
			}
		}
	}
}